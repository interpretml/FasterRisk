# Required
version: 2

# Image to use
build:
  os: ubuntu-22.04
  image: testing

# Configuration
python:
  version: 3.8
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
